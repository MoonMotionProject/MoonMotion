using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Delegates:
// • declares delegates for return types that have previously been needed in solution implementations, for a couple of reasons:
//   · so that they may be reused
//   · to provide the cleanest possible call
public static class Delegates
{
	public delegate void Void();
}